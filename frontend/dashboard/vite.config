import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  // Serve the dashboard under /dashboard/ so asset URLs resolve correctly
  base: '/dashboard/',
  // Use the `public/` directory for static assets (index.html, favicon, etc.)
  publicDir: 'public',
  build: {
    outDir: 'dist',         // Output build to `dist/`
    assetsDir: 'assets',    // Place JS/CSS/images under `assets/`
    emptyOutDir: true,
    rollupOptions: {
      input: 'public/index.html',
      output: {
        entryFileNames: 'assets/[name]-[hash].js',
        chunkFileNames: 'assets/[name]-[hash].js',
        assetFileNames: 'assets/[name]-[hash].[ext]'
      }
    }
  },
  server: {
    port: 3000,
    open: true,
    proxy: {
      // Proxy API requests to the FastAPI backend
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        secure: false,
      }
    }
  }
});
